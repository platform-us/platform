{
  "name": "platform",
  "version": "1.0.0",
  "main": "server/index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd server && yarn start",
    "dev": "concurrently \"nodemon server --ignore 'client'\" \"cd client && yarn start\"",
    "format": "prettier --write client/src/ && prettier --write server/"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4",
    "prettier": "^2.0.5"
  }
}
