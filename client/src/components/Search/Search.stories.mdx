import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Search from './Search';
import SearchBar from './SearchBar';

import '../../index.css';

<Meta title="Search" component={Search} />

# Search component

A popup that allows the user to search for policies at platforms, along with the accompanying search bar button for the navigation bar.

## Preview

<Preview>
  <Story name="Search bar">
    <div style={{ margin: 10 }}>
      <SearchBar />
    </div>
  </Story>
  <Story name="Search">
    <Search isOpen={true} close={() => console.log('close')} />
  </Story>
</Preview>

## Props

### Search bar

<Props of={SearchBar} />

### Search Popup

<Props of={Search} />
