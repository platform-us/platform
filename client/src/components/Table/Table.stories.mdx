import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Table from './Table';

import '../../index.css';

<Meta title="Table" component={Table} />

# Table component

Optionally give each row a component and then it will automatically wrap the cell content in that component.

TODO: Sorting columns

## Preview

<Preview>
  <Story name="Table">
    <div style={{ margin: 10 }}>
      <Table
        header={[
          { title: 'Name' },
          {
            title: 'Handle',
            component: ({ children }) => <code>@{children}</code>,
          },
          {
            title: 'Based?',
            component: ({ children }) =>
              children ? <p>BASED</p> : <p>NOT BASED</p>,
          },
        ]}
        rows={[
          {
            id: 1,
            cells: [
              { id: 1, value: 'James Medlock' },
              { id: 2, value: 'jdcmedlock' },
              { id: 3, value: true },
            ],
          },
          {
            id: 2,
            cells: [
              { id: 1, value: 'Liz' },
              { id: 2, value: 'pseudocia' },
              { id: 3, value: true },
            ],
          },
          {
            id: 3,
            cells: [
              { id: 1, value: 'Donald John Trump' },
              { id: 2, value: 'realdonaldtrump' },
              { id: 3, value: false },
            ],
          },
        ]}
      />
    </div>
  </Story>
</Preview>

## Props

IDs are used for React keys, they can be anything. They also only need to be unique per row.

<Props of={Table} />
