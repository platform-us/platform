import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { StaticRouter } from 'react-router-dom';
import { UserContext } from '../../App';
import Nav from './Nav';

import '../../index.css';

<Meta title="Nav" component={Nav} />

# Nav component

Displays either log in buttons or "My Platforms", depending on if the `user` prop is supplied

## Preview

<Preview>
  <Story name="Nav (signed out)">
    <StaticRouter>
      <Nav />
    </StaticRouter>
  </Story>
  <Story name="Nav (signed in)">
    <StaticRouter>
      <UserContext.Provider
        value={{ username: 'Mr Test', email: 'test@test.com', id: '69' }}
      >
        <Nav />
      </UserContext.Provider>
    </StaticRouter>
  </Story>
</Preview>

## Props

<Props of={Nav} />
